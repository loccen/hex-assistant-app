# release 验证记录

本文记录本 worktree `/home/code/hex-assistant-wt-resource-bundle` 的 release 资源补齐和打包验证结果。只记录真实执行过的命令和环境，不把 WSL 结果写成 Windows 局内验收通过。

## 当前记录

| 项目 | 记录 |
| --- | --- |
| worktree | `/home/code/hex-assistant-wt-resource-bundle` |
| 分支 | `agent/resource-bundle` |
| 记录时间 | 2026-04-30T03:10:41+08:00 |
| 验证时 HEAD | `2b9af0c` |
| 测试环境 | WSL Ubuntu，`mise` 管理 Node/Rust |
| 发布结论 | OCR 模型和 Windows ORT 资源已补齐，WSL 构建与 release 导出通过；Windows 发布和局内流程未验收 |

## 命令验证结果

| 项目 | 命令 | 当前结果 | 说明 |
| --- | --- | --- | --- |
| 空白检查 | `git diff --check` | 通过 | 未发现 diff 空白错误 |
| 依赖安装 | `mise exec -- npm install` | 通过 | 安装 74 个 npm 包 |
| 前端构建 | `mise exec -- npm run build` | 通过 | 生成 `dist/index.html` 和静态资源 |
| Rust 检查 | `mise exec -- cargo check --manifest-path src-tauri/Cargo.toml` | 通过 | Tauri Rust 工程检查通过 |
| Rust 测试 | `mise exec -- cargo test --manifest-path src-tauri/Cargo.toml` | 通过 | 112 项 Rust 测试通过 |
| release 导出脚本 | `mise exec -- npm run release:zip` | 通过 | 生成 `release/hex-assistant-release-*.zip`，包含 27 个文件 |
| Tauri Linux deb 构建 | `mise exec -- npm run tauri build -- --bundles deb` | 通过 | 生成 Linux `.deb`，不代表 Windows 安装包通过 |

## 本次产物

| 类型 | 路径 | SHA256 |
| --- | --- | --- |
| Linux 可执行文件 | `src-tauri/target/release/hex-assistant-app` | 未单独记录 |
| Linux deb | `src-tauri/target/release/bundle/deb/LOL 海克斯助手_0.1.0_amd64.deb` | `4cc51f75ff5086dee0c0add3f5869e90a42e561f8ab269fd123776fb2a3c7476` |
| PP-OCRv4 rec 模型 | `src-tauri/resources/models/ppocrv4_rec.onnx` | `48fc40f24f6d2a207a2b1091d3437eb3cc3eb6b676dc3ef9c37384005483683b` |
| ONNX Runtime DLL | `src-tauri/resources/onnxruntime/onnxruntime.dll` | `581b3c87f7944a3cdeeaa3dd048e3f4261aa4dc777a57ea154674ece1d04c575` |
| ONNX Runtime providers DLL | `src-tauri/resources/onnxruntime/onnxruntime_providers_shared.dll` | `61066a274b3c801af8fcadbb9e2699b40f6d33f55dc9487270bb6daa5799adf7` |
| release zip | `release/hex-assistant-release-*.zip` | 以最终导出产物和 zip 内 `checksums.txt` 为准 |

release zip 内容已检查，包含：

- `dist/`
- `docs/`
- `release/README.md`
- `release/已知限制.md`
- `release/诊断包导出说明.md`
- `release/验证记录.md`
- `src-tauri/resources/dictionaries/`
- `src-tauri/resources/models/README.md`
- `src-tauri/resources/models/ppocrv4_rec.onnx`
- `src-tauri/resources/onnxruntime/README.md`
- `src-tauri/resources/onnxruntime/onnxruntime.dll`
- `src-tauri/resources/onnxruntime/onnxruntime_providers_shared.dll`
- `src-tauri/resources/onnxruntime/ONNXRUNTIME-LICENSE`
- `src-tauri/resources/onnxruntime/ONNXRUNTIME-ThirdPartyNotices.txt`
- `src-tauri/resources/onnxruntime/ONNXRUNTIME-VERSION`
- `packages/bundle/deb/LOL 海克斯助手_0.1.0_amd64.deb`
- `packages/hex-assistant-app`
- `release-manifest.json`
- `checksums.txt`

## 资源状态

| 资源 | 当前状态 | 说明 |
| --- | --- | --- |
| OCR 词库 | 已包含 | `src-tauri/resources/dictionaries/augments.zh-CN.json` |
| OCR 模型 | 已包含真实模型 | `src-tauri/resources/models/ppocrv4_rec.onnx` 已随包资源提供，来自本机 RapidOCR PP-OCRv4 rec 模型；Windows 真实加载仍需验收 |
| ORT 动态库 | 已包含真实动态库 | 来自官方 `https://github.com/microsoft/onnxruntime/releases/download/v1.25.0/onnxruntime-win-x64-1.25.0.zip`，`onnxruntime.dll`、包内 `onnxruntime_providers_shared.dll` 和 ONNX Runtime 许可说明已随包资源提供；Windows 真实加载仍需验收 |
| Windows exe/msi | 未验收 | 需要 Windows runner 或交叉编译条件 |

## Windows 未验收项

以下项目必须在真实 Windows 桌面环境单独验收，当前 WSL 不能替代：

- Windows `exe` / `msi` 安装包生成。
- 干净 Windows 环境启动。
- OCR 模型从发布包资源目录加载。
- `onnxruntime.dll` 从发布包资源目录加载。
- LOL 无边框模式真实截图、黑屏检测和旧帧检测。
- Overlay 透明、置顶、不抢焦点和点击穿透。
- 多显示器、Windows 缩放和游戏 UI 缩放下的坐标正确性。
- 3 / 7 / 11 / 15 级局内端到端流程。
