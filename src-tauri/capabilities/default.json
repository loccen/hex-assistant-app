{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "hex-assistant-overlay"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
