# release 验证记录

本文记录本 worktree `/home/code/hex-assistant-wt-final-release` 的 release 线验证结果。只记录真实执行过的命令和环境，不把 WSL 结果写成 Windows 局内验收通过。

## 当前记录

| 项目 | 记录 |
| --- | --- |
| worktree | `/home/code/hex-assistant-wt-final-release` |
| 分支 | `agent/release-final` |
| 记录时间 | 2026-04-30T02:37:42+08:00 |
| 验证时 HEAD | `cc64666` |
| 测试环境 | WSL Ubuntu，`mise` 管理 Node/Rust |
| 发布结论 | WSL 构建与 release 导出通过；Windows 发布和局内流程未验收 |

## 命令验证结果

| 项目 | 命令 | 当前结果 | 说明 |
| --- | --- | --- | --- |
| 空白检查 | `git diff --check` | 通过 | 未发现 diff 空白错误 |
| 依赖安装 | `mise exec -- npm install` | 通过 | 安装 74 个 npm 包 |
| 前端构建 | `mise exec -- npm run build` | 通过 | 生成 `dist/index.html` 和静态资源 |
| Rust 检查 | `mise exec -- cargo check --manifest-path src-tauri/Cargo.toml` | 通过 | Tauri Rust 工程检查通过 |
| Rust 测试 | `mise exec -- cargo test --manifest-path src-tauri/Cargo.toml` | 通过 | 32 个 lib 单元测试及各模块测试通过 |
| release 导出脚本 | `mise exec -- npm run release:zip` | 通过 | 生成 `release/hex-assistant-release-*.zip`，包含 21 个文件 |
| Tauri Linux deb 构建 | `mise exec -- npm run tauri build -- --bundles deb` | 通过 | 生成 Linux `.deb`，不代表 Windows 安装包通过 |

## 本次产物

| 类型 | 路径 | SHA256 |
| --- | --- | --- |
| Linux 可执行文件 | `src-tauri/target/release/hex-assistant-app` | 未单独记录 |
| Linux deb | `src-tauri/target/release/bundle/deb/LOL 海克斯助手_0.1.0_amd64.deb` | `a0337c83595165765219fe86e870712d424a28c96e1f8e6c7a6e209fb9fdcc23` |
| release zip | `release/hex-assistant-release-*.zip` | 以最终导出产物和 zip 内 `checksums.txt` 为准 |

release zip 内容已检查，包含：

- `dist/`
- `docs/`
- `release/README.md`
- `release/已知限制.md`
- `release/诊断包导出说明.md`
- `release/验证记录.md`
- `src-tauri/resources/dictionaries/`
- `src-tauri/resources/models/README.md`
- `src-tauri/resources/onnxruntime/README.md`
- `packages/bundle/deb/LOL 海克斯助手_0.1.0_amd64.deb`
- `packages/hex-assistant-app`
- `release-manifest.json`
- `checksums.txt`

## 资源状态

| 资源 | 当前状态 | 说明 |
| --- | --- | --- |
| OCR 词库 | 已包含 | `src-tauri/resources/dictionaries/augments.zh-CN.json` |
| OCR 模型 | 未包含真实模型 | 当前只有 `src-tauri/resources/models/README.md`，发布 Windows 包前需补 `ppocrv4_rec.onnx` |
| ORT 动态库 | 未包含真实动态库 | 当前只有 `src-tauri/resources/onnxruntime/README.md`，发布 Windows 包前需补 `onnxruntime.dll` 和必要依赖 |
| Windows exe/msi | 未验收 | 需要 Windows runner 或交叉编译条件 |

## Windows 未验收项

以下项目必须在真实 Windows 桌面环境单独验收，当前 WSL 不能替代：

- Windows `exe` / `msi` 安装包生成。
- 干净 Windows 环境启动。
- OCR 模型从发布包资源目录加载。
- `onnxruntime.dll` 从发布包资源目录加载。
- LOL 无边框模式真实截图、黑屏检测和旧帧检测。
- Overlay 透明、置顶、不抢焦点和点击穿透。
- 多显示器、Windows 缩放和游戏 UI 缩放下的坐标正确性。
- 3 / 7 / 11 / 15 级局内端到端流程。
